수학 함수
round(숫자, 자릿수) : 원하는 자릿수만큼 반올림
truncate(숫자, 자릿수) : 원하는 자릿수까지만 보여주고 나머지는 삭제(절삭)
ceil(숫자) : 해당 숫자 올림
floor(숫자) : 해당 숫자 내림
MOD(숫자, 숫자) : 나머지를 구함, MOD(5, 3) => 2
Power(숫자, 숫자) : Power(2, 10), => 2의 10승 구함
		power(2, 3) => 2 * 2 * 2
To_Number(문자열) : 문자열을 숫자 타입으로 변경

abs(음수) : 절댓값 계산